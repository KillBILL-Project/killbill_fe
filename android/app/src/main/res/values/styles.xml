<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:statusBarColor">#ffffff</item>

        <item name="android:windowSplashScreenBackground" tools:targetApi="S">#000000</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="S">@drawable/empty_icon</item>
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="S">#000000</item>
        <item name="android:windowSplashScreenAnimationDuration" tools:targetApi="S">0</item>
    </style>

</resources>
